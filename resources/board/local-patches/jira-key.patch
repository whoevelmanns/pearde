diff --git a/resources/board/view.js b/resources/board/view.js
index c5dff9f..b160599 100644
--- a/resources/board/view.js
+++ b/resources/board/view.js
@@ -142,6 +142,15 @@ const fmtW = w => K() ? fmtHr(w * K())
   : (Math.round(w * 10) / 10 + "w").replace(".0w", "w");
 const esc = s => String(s).replace(/[&<>"']/g,
   c => ({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
+// LOCAL-ONLY, never synced to pearde-src or upstream: same shape as
+// jira_sync.py's issue_key() - a leading <PROJECT>-<number> on the PRD
+// dir's own basename, case-folded to upper. No API call.
+const JIRA_KEY_RE = /^([A-Za-z]+)-(\d+)(?:-|$)/;
+const jiraKey = rel => {
+  const base = (rel || "").split(/[\\/]/).pop() || "";
+  const m = JIRA_KEY_RE.exec(base);
+  return m ? `${m[1].toUpperCase()}-${m[2]}` : null;
+};
 const reduced = matchMedia("(prefers-reduced-motion: reduce)").matches;
 
 /* how long a worker has held this PRD, off the `claim:` stamp. Computed in
@@ -2572,6 +2581,8 @@ class PeardeBoard extends LitElement {
       ><div class="t">${t && t.critical ? html`<span class="star">★ </span>` : ""
       }${r.title || r.name}</div><div class="m">${
         r.board ? html`<span class="chip">${r.board}</span>` : ""
+      }${
+        jiraKey(r.rel) ? html`<span class="chip">${jiraKey(r.rel)}</span>` : ""
       }<span>p${r.prio}</span>${
         r.weight ? html`<span>${fmtW(r.weight)}</span>` : ""}${
         this.served && r.state === "open" && ADAPTERS.length === 1 ? html`<button class="start"
