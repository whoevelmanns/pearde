# Where the pearde skill stands

*2026-08-28, end of day*

The board runs itself now — the whole of what was promised this morning is
landed and committed, and every one of the repo's checks is green on the
result. `pearde` is one command in front of every tool. Moving a piece of
work between states is a command that checks its own rule and refuses with
the rule named. Spec files become "ready to build" by command, and a piece
too big for one sitting splits into children by command. Finished work is
closed by one call that runs the checks and commits only that work's own
changes — rebuilt exactly, and proven in place before any commit, after the
first version was caught putting a line in the wrong place. A worker's brief
is printed, never composed. A board exists after one command that asks
nothing, and the three lines it prints run as printed. A board says where it
is going and the plan orders toward it — on every board, with the old script
on the master board retired. The live page shows what the session is doing
and which workers have gone quiet. The round itself is one page a third of
its former length, written as the calls it makes. The cost of each round is a
number on the page. And a newcomer has a README that gets them to a running
board in sixty seconds, proven end to end. Fifteen pieces, all landed.

The workflow routes are finished except their door, and they were followed
thirty-three times today; every "what went wrong and what to do" row in them
was paid for by a real run.

## In work

Nothing of this stream. Two pieces of the workflow stream remain and both
are cleared to start: the door to the routes, and something that runs the
repo's thousand checks so a green result no longer depends on someone
asking.

## Planned

- **The health check's false problem on a valid group of boards** — parked,
  nobody on it, as decided earlier.

## Undecided or failing

**One thing waits on you.** A block of work sits uncommitted in this repo
that nobody working today owns — a names column and an answered-questions
panel for the page, about four hundred lines across five files, plus a
paragraph in the interview rules and some scout data. It predates every
session that ran today and none of them touched it. Commit it, or drop it;
until then every commit here has to step around it.

Three calls made today without asking, each written down with what it beat:
the tool moves the states and the prose becomes its spec; a new board
defaults its language to English rather than asking; and who is working is
an environment variable exported once at install, not a file. All three are
in effect. One decision is open and recorded for you: what `collect` should
do when two people edit adjacent lines of one file.

Nothing is pushed — the commits are the board's, the push is yours.
