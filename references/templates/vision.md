---
vision: <one sentence — the destination>
# terminals:            # the PRDs whose completion IS the vision — `<prd>`,
#   - <prd>             #   `@<member>/<prd>` on a master, `@<name>/<prd>` for
#   - @<member>/<prd>   #   the master's own. None means no axis: the board
#                       #   orders by dependency, weight and priority alone
# edges:                # a dependency nobody wrote as needs:, usually across
#   - "<from> -> <to>"  #   boards — the same addresses as terminals
---
<!-- The plan reads only the frontmatter: `vision` prints on the scan, and
     `terminals` plus `edges` place every live PRD by how many serial hops
     separate it from one terminal. @references/parts/order.md. -->

# <The destination, in prose>

<What the board adds up to when every terminal is done — for the person who
opens the board and asks what the work is FOR. A paragraph, or a bullet per
terminal. No dates.>
